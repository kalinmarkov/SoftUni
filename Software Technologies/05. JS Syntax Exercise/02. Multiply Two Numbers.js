function multiplyNumbers(nums) {
    let firstNumber = Number(nums[0]);
    let secondNumber = Number(nums[1]);
    let result = firstNumber * secondNumber;
    console.log(result);
}

multiplyNumbers(['2', '3'])